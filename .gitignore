/target
*.swp
*.swo
.DS_Store
desktop_conversation/
web/dist/
web/node_modules/

# Sensitive — never commit
.env
.env.*
!.env.example

# tunaFlow local agent runtime
.omc/
docs/agents/agents.disabled/
docs/agents/*.pre-sentinel
docs/agentSessionHistory.md

# scripts 실행 로그 (스크립트 본체는 commit, 결과 로그는 제외)
scripts/*.log
