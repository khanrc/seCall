# v0.9.1 Release Notes

## Highlights

- `secall log --backend/--model` 추가
- `[log]` config 섹션 추가
- hard-coded LLM model default 상수화 + warning 로그
- `GET /api/config` / `PATCH /api/config/{section}` 추가
- Web `/settings` 추가
- `secall config llm show|set|where` 추가
